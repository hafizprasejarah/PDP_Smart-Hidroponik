export const YLinepH = (value) => {
    const max = 14;
    const min = 0;
    const stepSize = 1;

    return { max, min, stepSize };
};

export const YLineSuhu = (value) => {
    const max = 100;
    const min = 0;
    const stepSize = 10;

    return { max, min, stepSize };
};


export const YLineRain = (value) => {
    const max = 100;
    const min = 0;
    const stepSize = 10;

    return { max, min, stepSize };
};


export const YLineHumidity = (value) => {
    const max = 100;
    const min = 0;
    const stepSize = 10;

    return { max, min, stepSize };
};

export const YLineNutrition = (value) => {
    const max = 2000;
    const min = 0;
    const stepSize = 100;

    return { max, min, stepSize };
};


export const YLineWaterLevel = (value) => {
    const max = 200;
    const min = 0;
    const stepSize = 20;

    return { max, min, stepSize };
};


export const YLineSuhuAir = (value) => {
    const max = 100;
    const min = 0;
    const stepSize = 10;
    return { max, min, stepSize };
};
